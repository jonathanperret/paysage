#!/usr/bin/env node
var debug = require('debug')('paysage');
var server = require('../app');

server.listen(process.env.PORT || 3000, function() {
  console.log('Express server listening on port ' + server.address().port);
});

